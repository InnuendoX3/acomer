<h2>Edit restaurant <%= data[0].name %></h2>
<form action="/restaurants/edit/<%= data[0].id %>" method="post">

   <!-- Form for edit a restaurant -->
   <label for="name">Name</label>
   <input type="text" name="name" value="<%= data[0].name %>">
   <label for="address">Address</label>
   <input type="text" name="address" value="<%= data[0].address %>">
   <label for="rating">Rating</label>
   <input type="number" name="rating" value="<%= data[0].rating %>">

   <a href="/restaurants">Cancel</a>
   <button type="submit">Savito</button>

</form>